This program written to help verify the transactions to make sure bookkeeping is correct, built with Java.

To run this program:
        1. Open the report.txt file, input the data on the new line under Date, SKU, Price and Discount
        2. Each lines are only valid for one transaction
        3. Open the verify.txt file or check on the terminal to find the verified result